group 're.notifica.flutter'
version '2.0.0'

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.2'
    }
}

rootProject.allprojects {
    repositories {
        google()
        jcenter()
        maven {
            url "https://github.com/Notificare/notificare-mvn-repo/raw/master/releases"
        }
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 16
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    lintOptions {
        disable 'InvalidPackage'
    }
    compileOptions {
        sourceCompatibility = '1.8'
        targetCompatibility = '1.8'
    }
}

dependencies {
    api 're.notifica:notificare-core:2.0.7'
    compileOnly "re.notifica:notificare-location:2.0.7"
    compileOnly "re.notifica:notificare-beacon:2.0.7"
    compileOnly "re.notifica:notificare-scannable:2.0.7"
    implementation 'com.android.support:support-annotations:28.0.0'
}
